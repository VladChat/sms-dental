# Reference — HISTORICAL ONLY

⚠️ **These artifacts are NOT the approved design.** This folder holds the original
faithful recreation of the pre-redesign repository UI (the dark-navy marketing
site + the gray setup tool), kept only for diffing and historical context.

**Do not implement from anything in here.** The approved system is everything in
`design/recall/` *outside* this folder — see `../README.md`, `../HANDOFF.md`,
`../TOKENS.md`, `../COMPONENTS.md`, `../SCREENS.md`, and the `../screens/` mockups.

Notably superseded here:
- Dark-by-default marketing → replaced by the light-first Recall system.
- Aptos font dependency → replaced by Schibsted / Hanken / IBM Plex Mono.
- Two disconnected palettes → one unified token set.
- "Replies"/raw SMS framing → **Patient requests** (office-ready cards).
